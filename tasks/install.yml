# SPDX-FileCopyrightText: 2023 Julian-Samuel Gebühr
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Ensure Mobilizon paths exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0750"
    owner: "{{ mobilizon_uid }}"
    group: "{{ mobilizon_gid }}"
  with_items:
    - "{{ mobilizon_base_path }}"
    - "{{ mobilizon_config_path }}"
    - "{{ mobilizon_data_path }}"
    - "{{ mobilizon_tzdata_path }}"
    - "{{ mobilizon_uploads_path }}"

- name: Ensure Mobilizon support files installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/{{ item }}.j2"
    dest: "{{ mobilizon_base_path }}/{{ item }}"
    mode: "0640"
    owner: "{{ mobilizon_uid }}"
    group: "{{ mobilizon_gid }}"
  with_items:
    - env
    - labels
  register: mobilizon_support_files_result

- name: Run if self-building of Mobilizon container image is not enabled
  when: "not mobilizon_container_image_self_build | bool"
  block:
    - name: Ensure Mobilizon container image is pulled via community.docker.docker_image
      when: devture_systemd_docker_base_container_image_pull_method == 'ansible-module'
      community.docker.docker_image:
        name: "{{ mobilizon_container_image }}"
        source: "{{ 'pull' if ansible_version.major > 2 or ansible_version.minor > 7 else omit }}"
        force_source: "{{ mobilizon_container_image_force_pull if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
        force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else mobilizon_container_image_force_pull }}"
      register: mobilizon_container_image_pull_result_ansible_module
      retries: "{{ devture_playbook_help_container_retries_count }}"
      delay: "{{ devture_playbook_help_container_retries_delay }}"
      until: mobilizon_container_image_pull_result_ansible_module is not failed

    - name: Ensure Mobilizon container image is pulled via ansible.builtin.command
      when: devture_systemd_docker_base_container_image_pull_method == 'command'
      ansible.builtin.command:
        cmd: "{{ devture_systemd_docker_base_host_command_docker }} pull {{ mobilizon_container_image }}"
      register: mobilizon_container_image_pull_result_command
      retries: "{{ devture_playbook_help_container_retries_count }}"
      delay: "{{ devture_playbook_help_container_retries_delay }}"
      until: mobilizon_container_image_pull_result_command is not failed
      changed_when: "'Downloaded newer image' in mobilizon_container_image_pull_result_command.stdout"

- name: Run if self-building of Mobilizon container image is enabled
  when: "mobilizon_container_image_self_build | bool"
  block:
    - name: Ensure Mobilizon repository is present on self-build
      ansible.builtin.git:
        repo: "{{ mobilizon_container_image_self_build_repo }}"
        version: "{{ mobilizon_container_image_self_build_repo_version }}"
        dest: "{{ mobilizon_container_image_self_build_src_files_path }}"
        force: "yes"
      register: mobilizon_git_pull_results

    - name: Ensure Mobilizon container image is built
      community.docker.docker_image:
        name: "{{ mobilizon_container_image_self_build_name }}"
        source: build
        force_source: "{{ mobilizon_git_pull_results.changed if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
        force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else mobilizon_git_pull_results.changed }}"
        build:
          dockerfile: Dockerfile
          path: "{{ mobilizon_container_image_self_build_src_files_path }}"
          pull: true
          args:
      register: mobilizon_container_image_build_result

- name: Ensure Mobilizon container network is created via community.docker.docker_network
  when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
  community.docker.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ mobilizon_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"
  register: mobilizon_network_creation_result

- name: Ensure Mobilizon container network is created via ansible.builtin.command
  when: devture_systemd_docker_base_container_network_creation_method == 'command'
  ansible.builtin.command:
    cmd: >-
      {{ devture_systemd_docker_base_host_command_docker }} network create
      {% if devture_systemd_docker_base_ipv6_enabled %}--ipv6{% endif %}
      {{ devture_systemd_docker_base_container_networks_driver_options_string }}
      {{ mobilizon_container_network }}
  register: mobilizon_network_creation_result
  changed_when: mobilizon_network_creation_result.rc == 0
  failed_when: mobilizon_network_creation_result.rc != 0 and 'already exists' not in mobilizon_network_creation_result.stderr

- name: Ensure Mobilizon systemd service is present
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/mobilizon.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/{{ mobilizon_identifier }}.service"
    mode: "0644"
  register: mobilizon_systemd_service_result

- name: Determine whether Mobilizon needs a restart
  ansible.builtin.set_fact:
    mobilizon_restart_necessary: >-
      {{
        mobilizon_support_files_result.changed | default(false)
        or mobilizon_systemd_service_result.changed | default(false)
        or mobilizon_container_image_pull_result_ansible_module.changed | default(false)
        or mobilizon_container_image_pull_result_command.changed | default(false)
        or mobilizon_container_image_build_result.changed | default(false)
      }}
